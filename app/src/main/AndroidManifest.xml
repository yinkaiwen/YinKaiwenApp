<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.kevin.yinkaiwenapp">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name="yinkaiwenapp.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name="remote.TaskService"
            android:enabled="true"
            android:exported="true"
            android:label="task process"
            android:process=":taskProcess">
            <intent-filter>
                <action android:name="yinkaiwen.intent.taskservice"/>
            </intent-filter>
        </service>

        <service
            android:name="proxyremote.CallBackService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="yinkaiwen.intent.proxy"/>
            </intent-filter>
        </service>

    </application>

</manifest>